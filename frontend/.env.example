# QuoteTrack frontend configuration.
# Copy to .env and adjust as needed. All variables are optional.

# Where Vite proxies /api requests during dev (default http://localhost:8080).
VITE_API_PROXY_TARGET=http://localhost:8080